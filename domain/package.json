{
  "name": "domain",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "transpilation": "tsc",
    "clean": "cross-env rm -rf dist",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "npm run clean && npm run transpilation && npm run test"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "cross-env": "^7.0.3",
    "istanbul-lib-coverage": "^3.2.2",
    "istanbul-reports": "^3.1.6",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
